# ======================
# BACKEND CONFIGURATION
# ======================

# Supabase (Backend - Service Role for writes)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# FastText Model Settings
FASTTEXT_REPO_ID=facebook/fasttext-fr-vectors
FASTTEXT_FILENAME=model.bin
FASTTEXT_CACHE_DIR=./.cache/fasttext

# CORS Settings (comma-separated origins, or "*" for all)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ======================
# FRONTEND CONFIGURATION
# ======================
# These are passed as build args in Docker

# Supabase (Frontend - Anon Key for reads)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# API URL (adjust for production)
# For local dev: http://localhost:8081
# For Docker local: http://embedding-api:8081 (internal) or http://localhost:8081 (external)
# For production: https://your-api-domain.com
VITE_API_URL=http://localhost:8081

# Analytics (optional - only needed in production)
# Supports Umami, Plausible, or similar analytics providers
VITE_ANALYTICS_SRC=https://your-analytics.example.com/script.js
VITE_ANALYTICS_ID=your-website-id
